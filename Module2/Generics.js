"use strict";
{
    const numberList = [30, 20, 50];
    //above code using generices
    const numberListGenerics = [30, 50, 20];
    //dynamic generices
    const numberListGenericsDynamic = [30, 50, 20];
    const nameList = ['x', 'y', 'z'];
    //above code using generices
    const nameListGenerics = ['x', 'y', 'z'];
    //dynamic generices
    const nameListGenericsDynamic = ['x', 'y', 'z'];
    const boolList = [true, false, true];
    //above code using generices
    const boolListGenerics = [true, false, true];
    //dynamic Generices 
    const boolListGenericsDynamic = [true, false, true];
    const userList = [
        {
            name: 'rasel',
            age: 26,
            contactNo: '0190000'
        },
        {
            name: 'piklu',
            age: 45,
            contactNo: '0189899999'
        }
    ];
    const couple = ['Mr. x', 'Mr. y'];
    const userWithId = [101, { name: 'x', age: 10, address: 'ctg' }];
}
